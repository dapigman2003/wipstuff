<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageReference Include="MSTest.TestAdapter" Version="4.3.2" PrivateAssets="all" />
    <PackageReference Include="MSTest.TestFramework" Version="4.3.2" />
    <PackageReference Include="Mono.Cecil" Version="0.11.6" />
    <!--
      Step 27.0.21 real-Harmony fixture acquisition is intentionally outside MSBuild.
      scripts/test.sh downloads the exact tagged Harmony-Fat 2.4.2 release archive into
      artifacts/host-step27-fixtures and passes only its net9.0/0Harmony.dll structural-surrogate path
      through STS2_STEP27_REAL_HARMONY_FIXTURE, with the selected archive member plus exact release/DLL SHA-256 pins providing provenance. Merged AssemblyRef rows are intentionally not used as target-framework evidence. No Harmony package assets enter this graph.
    -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/StS2Launcher.Core/StS2Launcher.Core.csproj" />
    <ProjectReference Include="../../fixtures/StS2Launcher.Step16.Fixture/StS2Launcher.Step16.Fixture.csproj" />
  </ItemGroup>


</Project>
