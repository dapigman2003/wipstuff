# Documentation

Current candidate: **Step 35.0.31 / Step 36.0.2 — 0.0.156 (156)**.

Physical 0.0.155 proves the exact receipt-backed PCK mount and `res://localization/eng` handoff. Step 36 remains open because the unchanged exact `ExecuteEssential` call then throws through a nested reflection exception chain. 0.0.156 changes telemetry only and is intended to reveal the first internal essential-initialization failure without broadening execution.

See `CURRENT-STATUS.md`, `MASTER-PLAN.md`, `REGRESSION-CONTRACTS.md`, and `history/INDEX.md` for the authoritative resumption path.
