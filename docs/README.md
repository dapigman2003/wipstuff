# Documentation index — active Steps 58–62 / 0.0.202

Active candidate: **0.0.202 (202)**. Physical authority remains formally closed through Step 57. Physical **0.0.201** is the latest Step-59 evidence: the retained embark `NConfirmButton` is ready/in-tree with all audited direct fields populated, yet the original transition reproducibly fails in `NConfirmButton.OnEnable()`; after failure `_isEnabled=true`, the character-select logical stack is exact-retained, and a one-player `StartRunLobby` exists.

0.0.202 preserves the proven PackedScene compatibility path and maximizes information per compilation. One IPA includes Step 59D plus new **59R/59U/59E** branches and an internal `STEP59TRACE_*` callback bridge that brackets the actual selected sts2 callsites only while an explicit Step-59 experiment is armed. 59R is read-only getter/icon-lookup rehearsal; 59U isolates `NButton.UpdateControllerButton()`; 59E isolates the retained embark `Enable()` path; the normal Step 59 retains the original game-owned transition and post-failure deck. Mutating probes/real Step 59 require fresh processes. Steps 60–62 remain unavailable unless the real transition closes cleanly; Step 63 remains unopened.

Primary authority:

- `CURRENT-STATUS.md` — current physical frontier and exact device sequence.
- `MASTER-PLAN.md` — product objective, authority model, and roadmap.
- `TESTING.md` — static/Codemagic/device procedure for 0.0.202 and its reusable Step-59 experiment branches.
- `RELEASE-CHECKLIST.md` — release-quality and packaging requirements.
- `REPORTS.md` — evidence surface contracts.
- `REGRESSION-CONTRACTS.md` — protected historical contracts.
- `history/INDEX.md` — evidence/design provenance.

Physical 0.0.193–0.0.198 progressively localized the Step-59 transition and instanced-root unique-name loss. 0.0.199 introduced the narrow GodotSharp `PackedScene.Instantiate` compatibility boundary; 0.0.200 physically proved that correction and localized the next failure to embark `NConfirmButton.OnEnable()`. Physical 0.0.201 then eliminated the obvious retained-button field/readiness hypotheses and showed the partial game-owned transition is real. 0.0.202 instruments the remaining base-button/controller/tween frontier while precompiling multiple cheap follow-up phone experiments.
