# Documentation index — active Steps 43–50 / 0.0.179

Active candidate: **0.0.179 (179)** — execution-aware direct main-menu startup ladder.

Physical Step 42 / 0.0.175 is closed positive 4/4: exact `NGame.InitPools()` returned once from a 22-method zero-boundary closure with frozen state/native confinement.

Physical 0.0.176 localized the first Step-45 singleton metadata issue. Physical 0.0.177 proved the corrected 331-method/6-approved-boundary local-save map and the real one-shot save call sequence, then stopped only on one planned host-framework binding. Physical **0.0.178 closes Step 45 4/4** because Step 46 Gate A accepted same-process local-save authority with state 2, rendering frozen, and zero post-Step-45 drift. The same run mapped exact `LaunchMainMenu(bool)` / async `MoveNext` metadata and then stopped safely in the old over-broad Step-46 transitive audit without invoking `LaunchMainMenu` or restarting rendering.

0.0.179 keeps Steps 43–45 unchanged, makes Step 46.1 a non-invoking execution-opcode-qualified immediate/deferred frontier map, and adds a direct main-menu resource/scene ladder through Steps 47–50. The direct route prepares exact PCK resources privately, instantiates exact `NMainMenu` off-tree, admits it once under the retained real `NGame.RootSceneContainer` while frozen, and finally permits one audited bounded render pulse that refreezes before evaluation.

Run **43 → 44 → 45 → 46 → 47 → 48 → 49 → 50** only after the immediately preceding rung is 4/4. Stop on the first failure and preserve that rung's reports. Steps 43–49 keep rendering frozen; Step 50 alone may pulse rendering.

Authoritative status: `CURRENT-STATUS.md`. Design provenance: `history/steps/STEP-46.1-50.0-DIRECT-MAIN-MENU-LADDER.md`.
