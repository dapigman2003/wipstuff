# Step 48.2–52.0 — retained RootSceneContainer authority correction / 0.0.181

Physical 0.0.180 preserved the direct-main-menu route through Step 47 and passed Step 48 Gate A/B. Gate C instantiated the exact real `NMainMenu` off-tree and then stopped safely during the runtime lifecycle-guard rehearsal because `NGame.RootSceneContainer` was null. No SceneTree admission or rendering occurred. The exact retained NGame hierarchy is already physically known to contain `/Game/RootSceneContainer` as `MegaCrit.Sts2.Core.Nodes.NSceneContainer`; therefore 0.0.181 removes the incorrect assumption that the property must already be populated before off-tree guard rehearsal.

Step 48.2 resolves the exact retained `/Game/RootSceneContainer` directly from the retained NGame child graph, requires exact path/type/direct-parent/inside-tree identity, and treats the NGame property as an observed nullable mirror only. A non-null property must reference that exact child; any other value fails closed. The rehearsal uses the exact child for root child-count invariants and still requires empty Godot command line, exact production SaveManager singleton/getter identity, exact Null-platform routing, no-drift `SetRichPresence`, no-drift off-tree `CheckCommandLineArgs`, state 2, and zero forbidden runtime drift. Step 48 also writes its preliminary Gate-B guard-shape map before arming the one-shot instantiation so future Gate-C failures retain useful map evidence.

Step 49 owns any compatibility repair. Gate B binds the exact retained child independently of the nullable property, binds the exact `NGame.set_RootSceneContainer(NSceneContainer)` runtime method, matches its runtime metadata token to the selected-image Cecil token, requires zero method calls, exactly one `NSceneContainer` field write on NGame, only trivial load/store/return opcodes, and zero external Cecil resolution. Gate C is already one-shot. If the property is null, and only then, Gate C assigns the exact retained child through that audited setter, requires the property getter to return the same object, requires no context/native drift, then performs the already-authorized frozen `RootSceneContainer.AddChild(NMainMenu)`. If the property is already non-null it must already be exact; a mismatched value is never overwritten.

Steps 50–52 are not expanded. Their prerequisite chain now continuously requires `NGame.RootSceneContainer` to remain the exact retained/repaired child. Step 50 remains the short audited render pulse/refreeze; Step 51 remains the non-invoking single-player frontier map; Step 52 remains the 1500 ms sustained audited render residency/refreeze. No new runtime boundary is introduced by 0.0.181.

Still forbidden: whole `GameStartup`, original `LaunchMainMenu`, single-player handler invocation, `DoCloudSync`, migration mutation, `InitializePlatform`, deferred startup/`ExecuteDeferred`, external/native Steamworks, FMOD/Spine native game extensions, explicit `_ExitTree`, arbitrary root-container replacement, and any repair to an object other than the exact retained `/Game/RootSceneContainer` child.
