# Step 57.0 scene-hint / PCK-resolution correction / 0.0.188

Physical 0.0.187 reached Step 57 from same-process closed Step-56 authority and stopped safely at Gate A before any PCK resource extraction. The final report was `FAIL — 0/4` with `Step 57.0 requires exactly one character-select TSCN candidate. observed=none`. The run ended normally with rendering frozen and explicitly performed no `OpenCharacterSelect`, `ResourceLoader`, `PackedScene` instantiation, or rendering.

This localizes a discovery-shape mismatch rather than a missing game resource. The 0.0.187 Step-56 mapper only retained immediate-closure strings that already had full `res://...*.tscn` form. The selected game image also carries the shorter exact scene identity `screens/character_select_screen`, while the supplied PCK path inventory contains the corresponding exact entry `res://scenes/screens/character_select_screen.tscn`.

0.0.188 keeps the runtime authority chain fail-closed and does **not** trust the external inventory as runtime input. Step 56 now preserves only the exact observed character-select scene hint in either supported form: `screens/character_select_screen` or `res://scenes/screens/character_select_screen.tscn`. Step 57 Gate A canonicalizes only those exact forms to `res://scenes/screens/character_select_screen.tscn` and then performs a read-only scan of the receipt-backed PCK directory, requiring exactly one matching directory entry under the already sealed PCK header/count authority. Gate B remains the byte-extraction boundary and still validates entry encryption/size bounds, directory MD5, SHA-256 and strict UTF-8 before textual risk mapping.

No `OpenCharacterSelect`, `ResourceLoader`, `PackedScene`, character-select scene execution/admission, rendering, trusted-install mutation, or Step 58 authority is added. Because the physical 0.0.187 run could enter Step 57 only after Step 56 4/4 closure in the same process, physical closure is now recorded through **Step 56**.
