# Steps 53–57 — single-player submenu continuation / 0.0.184

Physical authority is closed through Step 52. The user subsequently confirmed the 0.0.183 app works; no new Step-52 device report files were supplied, so 0.0.184 does not fabricate additional physical artifacts. The existing fresh-process closed-path runner remains intentionally capped at Step 52 and is the preferred way to reconstruct the known same-process authority before the new rungs.

0.0.184 opens only the next UI layer. **Step 53** isolates exact zero-argument `NMainMenu.OpenSingleplayerSubmenu()` from the broader Step-51 evidence. It records exact IL/token identity and runs the execution-opcode-qualified immediate/deferred audit under the already-physically-proven runtime guards. Unlike Step 51, Step 53 requires every immediate classified boundary to be admissible before it closes. `SingleplayerButtonPressed` remains evidence-only and is never invoked.

**Step 54** token-matches the exact runtime `OpenSingleplayerSubmenu()` method and binds the exact private `NMainMenu._singleplayerSubmenu` field as `NSingleplayerSubmenu`. The binding map is written before mutation. Gate C is one-shot: while rendering remains frozen, it invokes only exact `OpenSingleplayerSubmenu()` once, never the broad button handler. Success requires the same retained submenu object to be inside the SceneTree, `Visible=true`, `IsVisibleInTree=true`, and the selected compatibility/load-context/native authority to remain unchanged.

**Step 55** audits the actual visible `NSingleplayerSubmenu` subtree using the existing bounded menu-only node traversal and the same frame/input callback surface used by the physically closed main-menu render gates. The exact map is durable before a one-shot 750 ms requested render residency; the first managed continuation synchronously calls `StopRendering()` before telemetry/file I/O. Post-stop evidence accepts up to 5000 ms to tolerate a cold first frame while keeping the requested stop target unchanged. Gate D requires the submenu retained visible/in-tree and the renderer frozen.

**Step 56** keeps character select non-invoking. It binds exact zero-argument `NSingleplayerSubmenu.OpenCharacterSelect()`, records its IL, maps the execution/deferred frontier, and records every `res://` string literal reachable through the immediate same-module closure. Character-select `.tscn` candidates are called out separately. Classified boundaries are evidence; unresolved same-StS2 references or external Cecil resolution still fail closed.

**Step 57** is read-only PCK preflight. It requires exactly one character-select TSCN candidate (or one unambiguous `character_select`/`characterselect` candidate), reads that exact entry from the receipt-backed trusted PCK, validates the PCK directory MD5, records SHA-256/size/flags, decodes the resource as UTF-8 text, lists referenced `res://` paths, and counts textual Spine/FMOD/GDExtension/dylib/dll risk tokens. It never calls `OpenCharacterSelect`, `ResourceLoader`, `PackedScene.Instantiate`, or rendering. Risk findings authorize nothing; they exist to decide the next dedicated compatibility design.

Still unopened in 0.0.184: Step 58+, character-select resource loading/instantiation/admission, original `GameStartup`, original `LaunchMainMenu`, `DoCloudSync`, migration/archive mutation, `InitializePlatform`, external/native Steam APIs, deferred startup/`ExecuteDeferred`, trusted-install mutation, and native FMOD/Spine game extensions.
