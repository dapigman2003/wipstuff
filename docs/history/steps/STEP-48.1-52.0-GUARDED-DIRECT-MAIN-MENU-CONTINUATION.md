# Step 48.1–52.0 — guarded direct main-menu continuation / 0.0.180

Physical 0.0.179 closed Steps 46.1 and 47, then reached Step 48 Gate C. Exact main-menu `PackedScene.Instantiate(GenEditState.Disabled)` returned a real off-tree `NMainMenu`; the run stopped only because the static lifecycle frontier recursively treated conditional `_Ready()` paths as immediate authority requirements. The observed forbidden graph was rooted in three conditional surfaces: `NMainMenu.CheckCommandLineArgs()` multiplayer shortcuts, `SaveManager.get_Instance()` fallback construction, and `PlatformUtil.SetRichPresence()` routing. No `AddChild`, original `LaunchMainMenu`, native Steam, or rendering occurred, and the process ended normally.

0.0.180 does not approve those transitive Steam paths. Step 48.1 rehearses the exact concrete guards while the menu is still off-tree. It requires zero Godot command-line arguments; exact existing production `SaveManager._instance` with `_mockInstance == null`; exact `SaveManager.get_Instance()` returning the same instance; exact `PrimaryPlatform -> NullPlatformUtilStrategy`; exact `SetRichPresence("", "", null)` returning with no drift; and exact off-tree `CheckCommandLineArgs()` returning with root/menu child counts, SaveManager identity, NGame state, and resolver/host/private/initializer/rejected/native counts unchanged. Only those exact three calls are then represented as runtime-guarded immediate frontiers. All other classified immediate boundaries remain forbidden.

Step 49 retains one-shot frozen `RootSceneContainer.AddChild(NMainMenu)` but rechecks the Step-48 guard authority immediately before admission. Step 50 retains a fresh in-tree frame/input audit and short bounded render pulse/refreeze using the same guard authority.

Step 51 is a new non-invoking evidence rung. It binds exact `NMainMenu.SingleplayerButtonPressed` and `NMainMenu.OpenSingleplayerSubmenu`, records their IL, and maps their execution/deferred frontiers without invoking either method. Classified boundaries are evidence for a later explicit interaction design; unresolved same-sts2 or external Cecil resolution remains fatal.

Step 52 is a new bounded render-residency rung. It requires Step 51 4/4, performs a fresh actual in-tree frame/input audit, writes the map before rendering, then requests 1500 ms of real main-menu rendering with a 6000 ms evidence ceiling. The first managed continuation synchronously calls `StopRendering()` before telemetry/file I/O. No initializer-bearing, rejected-managed, or native-game-load escape is allowed, and Gate D freezes the captured post-pulse baseline.

Original whole `GameStartup`, original `LaunchMainMenu`, single-player handler invocation, deferred startup, Steam initialization/native Steam, cloud, and native FMOD/Spine remain unopened.
